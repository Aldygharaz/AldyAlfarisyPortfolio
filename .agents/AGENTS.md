# Project Custom Rules

## GitHub Workflow Rule
- **NEVER** automatically push changes to GitHub (`git push`) after completing a task or applying Kaizen improvements.
- Always commit locally and **WAIT** for explicit confirmation/signal from the user before executing a `git push`.
